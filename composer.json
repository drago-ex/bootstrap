{
	"name": "drago-ex/bootstrap",
	"type": "library",
	"description": "The `ExtraConfigurator` class extends the basic configuration functionality for the Nette Framework by adding the ability to search for `.neon` configuration files across directories and cache the results. It supports both development and production environments with automatic cache invalidation during development.",
	"license": ["MIT"],
	"authors": [
		{
			"name": "Zdeněk Papučík",
			"homepage": "https://github.com/accgit"
		}
	],
	"require": {
		"php": ">=8.3 <8.4",
		"nette/bootstrap": "^3.2",
		"nette/utils": "^4.0",
		"nette/caching": "^3.2",
		"nette/application": "^3.1"
	},
	"require-dev": {
		"nette/tester": "^2.5",
		"tracy/tracy": "^2.10",
		"phpstan/phpstan-nette": "^1.2.9"
	},
	"autoload": {
		"classmap": ["src/"]
	},
	"scripts": {
		"phpstan": "phpstan analyse --level 5 --configuration tests/phpstan.neon src",
		"tester": "tester tests -s"
	}
}
